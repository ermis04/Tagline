export class Location {
  getLocationData() {} // returns all the data related to location
  #calculateLocationProgress() {} // returns the progress of the user in the location
  getLocationPOIs() {} // returns all the POIs in the location
}
